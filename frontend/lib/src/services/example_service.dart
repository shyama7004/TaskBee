// Placeholder for a service class
class ExampleService {
  Future<String> fetchData() async {
    // Simulate a network call
    await Future.delayed(Duration(seconds: 2));
    return 'Fetched Data';
  }
}
